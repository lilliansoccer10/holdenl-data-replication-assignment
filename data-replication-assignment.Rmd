---
title: "Analysis Replication"
author: "Lillian Holden"
date: "2023-10-14"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# Install the required package
install.packages("readr")
# Load the installed Package
library(readr)
library(ggplot2)
library(dplyr)
```

```{r}
# xls files
my_data <- read_csv("/Users/lilyholden/Desktop/3_Distance_moved.csv", show_col_types = FALSE)
my_data
```
```{r}
Distance_moved_means <- my_data %>%  
  group_by(Tree_sp) %>%  
  summarize(Distance_moved_means=mean(Distance_moved_m))  
Distance_moved_means
```

```{r}
ggplot(Distance_moved_means, aes(x=Tree_sp, y=Distance_moved_means)) + 
geom_bar(stat="identity") 
```


  
ggplot(crop_means_se, aes(x=label, y=mean_N)) +  
geom_bar(stat="identity") +  
geom_errorbar(aes(ymin=lower_limit, ymax=upper_limit))